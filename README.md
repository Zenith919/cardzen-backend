# CARDZEN

Node.js + Express backend for card management, user authentication, and transactions.

## Features
- User registration/login (JWT protected)
- Card CRUD operations
- Purchase recording
- SQLite database

## Installation
1. Clone repository
2. Run `npm install`
3. Start server: `node server.js`
